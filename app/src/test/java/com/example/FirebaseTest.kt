package com.example

import org.junit.Test
import org.junit.Assert.*

class FirebaseTest {
    @Test
    fun testLogin() {
        // Just a dummy test to see if we can run tests
        assertTrue(true)
    }
}
